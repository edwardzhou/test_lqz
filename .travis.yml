
language: elixir
elixir: 
  - '1.6'
otp_release:
  - '20.2'

before_script:
  - mix local.rebar --force # for Elixir 1.3.0 and up
  - mix local.hex --force
  - mix deps.get
  - nvm install 6.2 && nvm use 6.2

script:
  - run: mix ecto.create
  - run: mix test